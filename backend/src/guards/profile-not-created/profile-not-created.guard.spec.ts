import { ProfileNotCreatedGuard } from './profile-not-created.guard';

describe('ProfileNotCreatedGuard', () => {
  it('should be defined', () => {
    expect(new ProfileNotCreatedGuard()).toBeDefined();
  });
});
